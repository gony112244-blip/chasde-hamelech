# חסדי המלך — תוכנית פיתוח מלאה
> קובץ זה הוא ה"מפה". בכל פגישת עבודה חוזרים אליו ועובדים לפיו בלבד.
> **כלל עבודה:** שלב נחשב גמור רק אחרי אישורך. אחרי כל שלב — בדיקה ביחד לפני שממשיכים.

---

## מצב נוכחי
- [ ] תוכנית מאושרת

---

## מבנה הפרויקט הצפוי

```
chasde-hamelech/
├── frontend/          ← React + Vite (כמו הפנקס)
│   ├── src/
│   │   ├── pages/     ← דפים ציבוריים
│   │   ├── admin/     ← ממשק ניהול
│   │   └── components/← רכיבים משותפים
│   └── package.json
├── backend/           ← Node.js + Express (כמו הפנקס)
│   ├── routes/
│   ├── db.js          ← העתק מהפנקס
│   └── server.js
├── .env
└── package.json
```

---

## שלבי הפיתוח

---

### שלב 1 — הקמת שלד הפרויקט
**דרישות מקדמיות:** Node.js מותקן. אין צורך ב-PostgreSQL.
**מה אני עושה לבד:** יוצר את תיקיית הפרויקט, מעתיק מבנה Vite+React מהפנקס, מגדיר Express בסיסי.

**בדיקה לאישורך:**
- [ ] `npm run dev` רץ ומציג דף ריק ב-localhost
- [ ] הנתיב `/` עובד בפרונטאנד
- [ ] הנתיב `/api/health` עובד בבקאנד ומחזיר `{ ok: true }`

**לא ממשיכים לשלב 2 לפני אישורך.**

---

### שלב 2 — עיצוב בסיסי (UI Foundation)
**דרישות מקדמיות:** שלב 1 מאושר. אין צורך ב-DB.
**מה אני עושה לבד:** פלטת צבעים (זהב, כחול-לילה, שמנת), פונט Heebo, RTL, Navbar, Footer, Mobile-first.

**מה אני צריך ממך לפני שמתחיל:**
- [ ] האם יש לוגו? (אם לא — נשתמש בטקסט בינתיים)
- [ ] האם יש תמונת פרופיל שלך לפינת הסופר? (אופציונלי)

**בדיקה לאישורך:**
- [ ] Navbar עם שם "חסדי המלך" נראה תקין במובייל
- [ ] הצבעים תואמים את החזון (זהב + כחול כהה)
- [ ] Footer עם פרטי יצירת קשר בסיסיים

**לא ממשיכים לשלב 3 לפני אישורך.**

---

### שלב 3 — דף הבית (Landing Page)
**דרישות מקדמיות:** שלב 2 מאושר. אין צורך ב-DB (נתונים hardcoded זמנית).
**מה אני עושה לבד:** Hero section, מדד החיוכים (מספרים זמניים), "איך עוזרים" (כפתורי Bit/PayBox), פינת הסופר.

**מה אני צריך ממך לפני שמתחיל:**
- [ ] **טקסט פינת הסופר** (גרסה שלך, לא של הגמיני) — חייב להיות בקולך האישי
- [ ] **מספרים אמיתיים:** כמה ילדים בערך בשנתיים? כמה בתי חולים? כמה ספרים?
- [ ] **לינק Bit/PayBox** של אחיך (או placeholder בינתיים)
- [ ] **הגדרת ה-Slogan:** "מחזירים את החיוך לגיבורים הקטנים" — מאשר?

**בדיקה לאישורך:**
- [ ] דף הבית נראה מלא ומרשים במובייל
- [ ] מדד החיוכים נקרא בבירור
- [ ] כפתורי הסיוע עובדים (מובילים ל-Bit/PayBox)
- [ ] פינת הסופר — מרגישה אישית ונכונה

**לא ממשיכים לשלב 4 לפני אישורך.**

---

### שלב 4 — קיר התודה
**דרישות מקדמיות:** שלב 3 מאושר. אין צורך ב-DB (mock data בינתיים).
**מה אני עושה לבד:** ממשק הצגת תודות, טופס הגשת תודה (שם/כינוי + הודעה), הודעת "ממתין לאישור".

**בדיקה לאישורך:**
- [ ] מישהו יכול להגיש תודה מהטלפון תוך 30 שניות
- [ ] ניתן להגיש בעילום שם
- [ ] נראה נחמד (לא "טופס מועדון")

**לא ממשיכים לשלב 5 לפני אישורך.**

---

### שלב 5 — טפסי מתנדבים + צור קשר
**דרישות מקדמיות:** שלב 4 מאושר. אין צורך ב-DB.
**מה אני עושה לבד:** טופס מתנדב (שם, טלפון, עיר, האם יש רכב), טופס צור קשר כללי.

**מה אני צריך ממך:**
- [ ] האם מתנדב שנרשם מקבל תגובה אוטומטית מיידית? מה כתוב בה?

**בדיקה לאישורך:**
- [ ] שני הטפסים עובדים ומציגים הודעת אישור
- [ ] Validation מסביר בעברית ("שדה חובה" וכו')

**לא ממשיכים לשלב 6 לפני אישורך.**

---

### שלב 6 — דף QR (Landing Page למובייל)
**דרישות מקדמיות:** שלב 5 מאושר. אין צורך ב-DB.
**מה אני עושה לבד:** דף ייעודי שנפתח בסריקת QR מבית החולים. ללא header/footer. מסר חם + כפתור "אמרו תודה" + כפתור "עזרו לנו".

**בדיקה לאישורך:**
- [ ] הדף נטען תוך פחות מ-3 שניות
- [ ] נראה נכון על מסך קטן (360px)
- [ ] המסר הראשון שרואים הוא חם — לא "תרמו כסף"

**לא ממשיכים לשלב 7 לפני אישורך.**

---

### שלב 7 — Admin Dashboard (ממשק ניהול)
**דרישות מקדמיות:** שלב 6 מאושר. אין צורך ב-DB עדיין.
**מה אני עושה לבד:** דף login פשוט (סיסמה אחת), Dashboard עם 4 כרטיסיות: גלריה, קיר תודה (מודרציה), פניות, עריכת תוכן.

**בדיקה לאישורך:**
- [ ] אחיך יכול להיכנס עם סיסמה
- [ ] הוא רואה את כל הכלים במסך אחד
- [ ] פשוט מספיק — לא מבלבל

**לא ממשיכים לשלב 8 לפני אישורך.**

---

### שלב 8 — חיבור מסד נתונים (PostgreSQL נדרש כאן)
**⚠️ כאן נדרש PostgreSQL מותקן.**
**דרישות מקדמיות:** שלבים 1–7 מאושרים. PostgreSQL מותקן.
**מה אני עושה לבד:** יצירת Schema, חיבור `db.js` (העתק מהפנקס), חיבור כל הטפסים ל-DB.

**מה אני צריך ממך לפני שמתחיל:**
- [ ] האם PostgreSQL מותקן? (נבדוק ביחד אם לא)
- [ ] פרטי DB (user, password, port)

**בדיקה לאישורך:**
- [ ] תודה שהוגשה ← מופיעה בטבלה
- [ ] פנייה שהוגשה ← מופיעה בטבלה
- [ ] Admin רואה את הנתונים האמיתיים

**לא ממשיכים לשלב 9 לפני אישורך.**

---

### שלב 9 — העלאת תמונות + גלריה
**דרישות מקדמיות:** שלב 8 מאושר.
**מה אני עושה לבד:** Admin יכול להעלות תמונה + כיתוב + שיוך לאירוע. גלריה ציבורית מסודרת לפי תיקיות. שימוש ב-multer + sharp (העתק מהפנקס).

**בדיקה לאישורך:**
- [ ] Admin העלה תמונה → מופיעה בגלריה הציבורית
- [ ] תמונות מסודרות לפי אירוע
- [ ] נראה יפה על מובייל

**לא ממשיכים לשלב 10 לפני אישורך.**

---

### שלב 10 — התראות ומיילים
**דרישות מקדמיות:** שלב 9 מאושר.
**מה אני עושה לבד:** מייל תודה אוטומטי לתורם/מתנדב (תבנית HTML מהפנקס + מיתוג חסדי המלך). כפתור "שלח תודה" ב-Admin.

**מה אני צריך ממך:**
- [ ] כתובת מייל השולח (`chasdehamelech@gmail.com`?)
- [ ] נוסח הודעת תודה — תכתוב, אני אעצב

**בדיקה לאישורך:**
- [ ] מייל תודה מגיע לתיבה האישית
- [ ] נראה מקצועי, לא ספאם

**לא ממשיכים לשלב 11 לפני אישורך.**

---

### שלב 11 — פריסה לאוויר (Deployment)
**דרישות מקדמיות:** שלב 10 מאושר.
**מה אני עושה לבד:** העלאה ל-Render (Frontend + Backend), חיבור דומיין.

**מה אני צריך ממך:**
- [ ] דומיין נרכש? (`.co.il` / `.org.il`)
- [ ] חשבון Render (נפתח ביחד אם אין)

**בדיקה לאישורך:**
- [ ] האתר נגיש בדומיין האמיתי
- [ ] QR Code שנסרק → מוביל לאתר החי

---

## מה לא נדרש ממך טכנית

| נושא | מי מטפל |
|------|---------|
| כל קוד React/Node | אני |
| Schema של DB | אני |
| עיצוב ו-CSS | אני |
| חיבורי API | אני |

## מה נדרש ממך (תוכן, לא קוד)

| שלב | מה צריך |
|-----|---------|
| שלב 3 | טקסט פינת הסופר, מספרים אמיתיים מהשטח, לינק Bit |
| שלב 5 | נוסח הודעת ברוכים הבאים למתנדב |
| שלב 8 | PostgreSQL מותקן + פרטי חיבור |
| שלב 10 | נוסח הודעת תודה, כתובת מייל |
| שלב 11 | דומיין, חשבון Render |

---

## שאלות לפני שלב 1

1. **PostgreSQL:** האם מותקן אצלך? (הרץ `psql --version` בטרמינל)
2. **Node.js:** האם מותקן? (הרץ `node --version`)
3. **שם תיקייה:** האתר יישמר ב-`C:\Users\User\OneDrive\Desktop\chasde-hamelech` — מסכים?
4. **Git:** האם תרצה Repo נפרד לפרויקט הזה?

---

*גרסה: 1.0 | נוצר: אפריל 2026 | ממתין לאישור לפני שלב 1*
